#include <stdio.h>

int main(void)
{
    printf("Enter your name: ");
    GetString() = string userName;
    printf("Enter your age: ");
    userAge = GetInt();
    int nextAge == userAge + 1;
    print("Hello, %d! You will be %d years old on your next birthday!\n", userName, userAge, nextAge);
}